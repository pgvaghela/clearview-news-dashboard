providers = ["python"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head && python3 scripts/seed_outlets.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
