[build]
buildCommand = "python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "alembic upgrade head 2>&1; echo \"Alembic exit $?\"; uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level debug 2>&1"
