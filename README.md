# ClearView News Dashboard

 > project focused on helping people compare news coverage quickly.

ClearView is an  app where I aggregate trending stories across the political spectrum, group coverage by political lean (Left / Lean Left / Center / Lean Right / Right) using AllSides bias ratings, and surface fact-checks and source context for each story. When no professional fact-check exists, Claude AI generates an analysis as a fallback.

**Live app:** [clearview-news-dashboard.vercel.app](https://clearview-news-dashboard.vercel.app)  
**API:** [clearview-backend-production.up.railway.app](https://clearview-backend-production.up.railway.app/health)

**Built by:** Priyansh Vaghela 

---

## What it does

I built ClearView to pull articles from 13 news outlets, cluster them into stories using TF-IDF and entity-overlap (~86% accuracy), and label each outlet's coverage by political lean. On the story page, you can read the same event as reported from the left, center, and right — side by side.

Each story includes:
- **AI summary** — a neutral overview generated by Claude AI
- **Lean badges** — color-coded by AllSides bias rating with a sourcing tooltip
- **Fact-check panel** — pulls from Google Fact Check Tools API; falls back to Claude AI when no professional check exists
- **Related coverage panel** — WebCite source search to find additional reporting

---

## Features

- Article ingestion from 13 outlets across all 5 lean categories (NewsAPI)
- TF-IDF + entity-overlap story clustering (~86% grouping accuracy)
- AllSides bias labeling with interactive lean tooltip
- Google Fact Check Tools API with per-story caching
- Claude AI fact-check fallback when Google returns no results
- Claude AI story summary per card
- WebCite "Related coverage" panel
- NYT-style dashboard and story page UI
- Error boundary and mobile layout
- Backend tests (pytest) — 10 passing
- Frontend tests (Vitest) — 25 passing

---

## Stack

| Layer | Tech |
|---|---|
| Frontend | React, Vite, TypeScript |
| Backend | FastAPI, Python |
| Database | PostgreSQL (Railway) |
| Clustering | TF-IDF + entity overlap (scikit-learn) |
| AI | Anthropic Claude API |
| Fact-checking | Google Fact Check Tools API |
| Deployment | Railway (backend + DB), Vercel (frontend) |

---

## Setup (local dev)

### Prerequisites
- [Docker Desktop](https://www.docker.com/products/docker-desktop/)
- Python 3.11+
- Node.js 20+

### 1. Clone and start the database

```bash
git clone https://github.com/pgvaghela/clearview-news-dashboard.git
cd clearview-news-dashboard

docker compose up -d
cp backend/.env.example backend/.env
# Add your API keys to backend/.env (see Keys section below)
```

The default `DATABASE_URL` in `.env.example` connects to the Docker Compose Postgres on **localhost:5433**.

### 2. Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt

alembic upgrade head            # run migrations
python scripts/seed_outlets.py  # load AllSides bias data

python scripts/ingest.py              # fetch articles from NewsAPI
python scripts/cluster_and_label.py  # cluster + label + generate summaries
python scripts/run_factchecks.py     # fetch fact-checks (Google + Claude AI fallback)

uvicorn app.main:app --reload --port 8000 --reload-dir app
```

API docs: http://localhost:8000/docs

### 3. Frontend

```bash
cd frontend
npm install
npm run dev
```

App: http://localhost:5173 — Vite proxies `/api` to `http://localhost:8000`.

### 4. Run tests

```bash
cd backend && pytest -v
cd frontend && npm run test
```

### API Keys (backend/.env)

| Variable | Source | Required for |
|---|---|---|
| `NEWSAPI_KEY` | [newsapi.org](https://newsapi.org) | article ingestion |
| `GOOGLE_FACTCHECK_API_KEY` | [Google Cloud Console](https://console.cloud.google.com) | fact-check lookup |
| `ANTHROPIC_API_KEY` | [console.anthropic.com](https://console.anthropic.com) | AI fact-check + summaries |
| `WEBCITE_API_KEY` | WebCite | related coverage panel |

Placeholder keys are detected and skip HTTP calls with a one-time warning — the API and UI boot without them.

---

## Architecture

```
  ┌─── EXTERNAL ────────────────────────────────────────────────┐
  │  NewsAPI    Google Fact Check API    Anthropic API (Claude)  │
  │  AllSides CSV                        WebCite API             │
  └─────────────────────────────────────────────────────────────┘
          │                    │
          ▼                    ▼
  ┌─── SYSTEM (Railway + Vercel) ───────────────────────────────┐
  │                                                              │
  │  [ingest.py]            ──►  PostgreSQL (Railway)            │
  │  [cluster_and_label.py]       ├── articles                   │
  │    TF-IDF clustering    ──────── stories                     │
  │    AllSides labeling    ──────── lean_labels / outlets       │
  │    AI summaries         ──────── stories.summary             │
  │  [run_factchecks.py]    ──────── fact_checks                 │
  │                                └── webcite_story_cache       │
  │                                                              │
  │  FastAPI (Railway)   GET /api/v1/stories                     │
  │                      GET /api/v1/stories/:id                 │
  │                      GET /api/v1/stories/:id/factchecks      │
  │                      GET /health                             │
  │                                                              │
  │  React + Vite (Vercel)                                       │
  │    Dashboard — featured story, sidebar, story grid           │
  │    StoryPage — L/C/R columns, lean tooltip, fact-check panel │
  └─────────────────────────────────────────────────────────────┘
```

---

## Project Structure

```
clearview/
├── docker-compose.yml
├── backend/
│   ├── app/
│   │   ├── api/routes.py
│   │   ├── core/config.py
│   │   ├── db/
│   │   │   ├── database.py
│   │   │   └── migrations/versions/
│   │   ├── models/models.py
│   │   ├── schemas/schemas.py
│   │   └── services/
│   │       ├── clustering.py
│   │       ├── labeling.py
│   │       ├── factchecks.py          # Google + Claude AI fallback
│   │       ├── summarize.py           # Claude AI story summaries
│   │       └── webcite.py
│   ├── scripts/
│   │   ├── ingest.py
│   │   ├── cluster_and_label.py
│   │   ├── run_factchecks.py
│   │   └── seed_outlets.py
│   ├── tests/test_api.py
│   ├── data/allsides_outlets.csv
│   ├── requirements.txt
│   └── railway.toml
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── ErrorBoundary.jsx
│   │   │   ├── FactCheckPanel.jsx
│   │   │   ├── LeanBadge.jsx
│   │   │   ├── LeanTooltip.jsx
│   │   │   ├── StoryCard.jsx
│   │   │   ├── StoryFactSources.jsx
│   │   │   └── WebcitePanel.jsx
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx
│   │   │   └── StoryPage.jsx
│   │   ├── hooks/useApi.js
│   │   └── services/api.js
│   └── vite.config.js
└── README.md
```
